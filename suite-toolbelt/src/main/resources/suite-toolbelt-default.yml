# SpringSuite toolbelt registry and executor configuration.
# Owned by suite-toolbelt.

suite:
  toolbelt:
    enabled: true
    # Tool descriptor scan roots. Relative paths are resolved from the SpringSuite runtime root.
    # By default SpringSuite reads only its own local tools from ./tools.
    scan-roots:
      - tools
    include-path-tools: true
    allow-execution: true
    default-timeout: 0s
    max-stdout-bytes: 0
    max-stderr-bytes: 0
    path-tools:
      - git
      - java
      - javac
      - python
      - py
      - cloudflared
      - gradle
