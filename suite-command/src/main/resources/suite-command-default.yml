# SpringSuite interactive command console configuration.
# Owned by suite-command.

suite:
  console:
    command:
      enabled: true
      prompt: "> "
      print-welcome: true
      allow-shutdown: true
      # Keep MCP/API availability stable: remote callers cannot terminate the runtime by default.
      allow-shutdown-over-api: false
